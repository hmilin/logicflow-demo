import { createFromIconfontCN } from '@ant-design/icons-vue'
import IconfontUrl from '~/assets/iconfont/iconfont.js?url'

const Icon = createFromIconfontCN({
  scriptUrl: IconfontUrl,
})

export default Icon
